# نموذج بيانات FiberOps

يوثق هذا النموذج البيانات التشغيلية الحساسة في FiberOps، ويعتمد على مبدأ فصل **السجل الأساسي للموظف** عن الوثائق والاعتمادات القابلة للتجديد. تحفظ التواريخ كتاريخ ميلادي موحد في قاعدة البيانات، بينما يظهر التطبيق تنبيهات الامتثال بناءً على قرب تاريخ الانتهاء.

| الكيان | الغرض | العلاقات الأساسية |
| --- | --- | --- |
| `employees` | الملف الأساسي للعامل وبيانات التواصل والوضع الوظيفي | ينتمي إلى قسم ومشروع اختياريين، ويرتبط بإقامة واحدة وعدة مؤهلات ووثائق وتكليفات. |
| `residencyPermits` | الإقامة ورقمها وحالتها وتاريخ انتهائها وتجديدها | سجل واحد لكل موظف مع سجل تجديد مرجعي وحالة امتثال. |
| `employeeQualifications` | شهادات السلامة والمهنية والفنية | عدة سجلات لكل موظف، مع جهة إصدار وتاريخ انتهاء وحالة. |
| `employeeDocuments` | الوثائق الداعمة مثل جواز السفر والتأشيرة والتأمين | عدة سجلات لكل موظف، ومرجع ملف اختياري للتخزين الخارجي. |
| `employeeAssignments` | التكليفات المرتبطة بالمشاريع | تربط موظفاً بمشروع ودور وفترة عمل وحالة. |
| `departments` و`projects` | تصنيف القوى العاملة ضمن البنية الإدارية والتنفيذية | تستخدم كمراجع اختيارية للموظف والأصول والتصاريح والمسارات. |
| `fiberDrums` و`fieldEquipment` | أصول شبكات الألياف ومعدات الميدان | ترتبط بمشروع أو موظف عند التخصيص. |
| `permits` و`workRoutes` | اعتمادات المسارات والتنفيذ الميداني | يمنع الاعتماد المنتهي من دعم المسارات التشغيلية. |
| `operationalAuditLogs` | سجل تدقيق غير قابل للاستبدال من واجهة التطبيق | يسجل المستخدم والكيان والإجراء وملخصاً وقيم ما قبل/بعد التغيير. |

## قواعد الامتثال

| الحالة | قاعدة التقييم | الأثر داخل التطبيق |
| --- | --- | --- |
| `valid` | التاريخ أبعد من 60 يوماً | يسمح بالتشغيل والتكليف. |
| `expiring` | المتبقي من 1 إلى 60 يوماً | يظهر ضمن التنبيهات ويحتاج متابعة. |
| `expired` | تاريخ الانتهاء اليوم أو قبله | يظهر كحالة حرجة ولا يجوز استخدامه كاعتماد صالح. |
| `under_renewal` | تم فتح إجراء التجديد ولم يكتمل | يبقى ظاهراً للمتابعة حتى اعتماد تاريخ الانتهاء الجديد. |

> تعتمد إجراءات الإنشاء والتعديل والحذف والتجديد والتعيين على صلاحية المدير. ويسجل التطبيق كل تغيير تنفيذي في `operationalAuditLogs` لتسهيل المراجعة الداخلية.
